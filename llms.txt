# x-account-cleaner / X Account Cleaner / twitter-auto-cleaner

> Open-source Playwright-based desktop tool for **cleaning an X / Twitter account locally**: bulk delete tweets and replies, undo retweets, unlike liked tweets, remove bookmarks, and manage following cleanup through export, classification, manual review, dry-run, and confirmed execution. Runs entirely on your own machine — automates the X web UI in a Chromium browser using your own logged-in session. Cross-platform (Windows / macOS / Linux), one-click install + start scripts, config-driven cleaning strategy, exponential-backoff retry, stable browser profile controls, run summaries, and progress tracking.

This is the X / Twitter counterpart to projects like `bilibili-cleaner`. Use cases: pre-account-deletion data wipe, side account cleanup, account hand-over, privacy sweep.

## Key docs

- [README.md](README.md): Feature list, prerequisites, three quick-start paths (Windows .bat, macOS/Linux .sh, npm), configuration reference, anti-detection notes.
- `install.bat` / `start.bat`: Windows one-click scripts.
- `install.sh`: macOS / Linux installer.
- `config.json` / equivalent: Strategy configuration.

## What this tool does

- **Delete every tweet** the account has posted
- **Delete every reply** the account has posted
- **Untag every retweet**
- **Unlike every liked tweet**
- **Remove every bookmark**
- **Following cleanup workflow**: export following list, classify candidates, manually review, dry-run, then execute a confirmed list
- **Login management**: automated login + manual login fallback, persist session
- **Config-driven**: customize cleaning strategy via config file
- **Smart retry**: exponential backoff on transient failures
- **Detailed logging**: full audit trail to file
- **Batch processing**: chunked operations to avoid X rate-limit triggers
- **Anti-detection**: emulates real-user browser fingerprint and pacing
- **Progress tracking**: real-time per-category counters

## What this tool does NOT do

- ❌ Delete someone else's account data — only the account you logged into
- ❌ Bypass X's terms of service (you still operate as your account, just programmatically) — verify your usage doesn't violate ToS in your jurisdiction
- ❌ Recover deleted tweets — operations are irreversible
- ❌ Touch DMs, lists, communities, or paid X features
- ❌ Send anything to a third-party server — Playwright session lives on your machine
- ❌ Work without Node.js — needs Node >= 18.18

## Common questions

- **会不会被 X 封号?** 风险存在但工具尽量降低 —— 分批处理 + 指数退避 + 反检测 fingerprint + 模拟真实用户节奏。但 X 政策随时可能变,**你自己承担风险**。建议先用小号试。
- **删过的推能恢复吗?** 不能。X 没有撤销机制。**先想清楚再跑**。
- **要不要 API Key / Developer 账号?** 不需要。工具走的是网页前端自动化(Playwright 控制浏览器),不调 X API。
- **登录状态保存在哪?** Playwright 的浏览器 user-data 目录,本地文件;关闭浏览器不会丢,主动删除目录可清。
- **能只清一类(比如只清点赞)吗?** 能。配置文件控制启用哪些清理任务。
- **macOS / Windows / Linux 都支持?** 都支持。Windows 有 `.bat` 一键脚本,macOS / Linux 用 `install.sh` + npm。

## Repository

- GitHub: https://github.com/tytsxai/x-account-cleaner (alias: twitter-auto-cleaner)
- License: see LICENSE
- Stack: Node.js >= 18.18 + Playwright + Chromium
- Platforms: Windows, macOS, Linux
- Author: tytsxai

## Useful search phrases

- X 账号清理, Twitter 账号清理, X 批量删除推文, Twitter 批量删除推文, X 批量取消点赞, Twitter 批量取消点赞, X 批量取消关注, Twitter 批量取消关注, X 批量删除回复, Twitter 批量删除回复, X 删书签, Twitter 删书签, X 注销前清理, Twitter 注销前清理, X 小号整理, Twitter 小号整理, X account cleanup tool, Twitter account wipe, bulk delete tweets, bulk unlike, bulk unfollow Twitter, Twitter account pre-deletion cleanup, X account data wipe, Playwright Twitter cleaner, open-source Twitter cleaner, alternative to redact for Twitter
